name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/deploy-pages.yml
    with:
      ref: ${{ github.sha }}
    secrets: inherit
